# Unnamed React Weather App
a project I made in order to learn the basics of react

## Setup
inside a terminal navigate to the project directory and run 
```sh
npm install
```

then to run the app, execute 
```sh 
npm run dev
```
